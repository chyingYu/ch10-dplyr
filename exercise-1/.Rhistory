# Install devtools package: allows installations from GitHub
install.packages('devtools')
# Install "fueleconomy" dataset from GitHub
devtools::install_github("hadley/fueleconomy")
# Use the `libary()` function to load the "fueleconomy" package
library(fueleconomy)
# You should now have access to the `vehicles` data frame
# You can use `View()` to inspect it
View('vehicle')
('vehicles')
# You should now have access to the `vehicles` data frame
# You can use `View()` to inspect it
View('vehicles')
# You should now have access to the `vehicles` data frame
# You can use `View()` to inspect it
View(vehicles)
colnames(vehicles)
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
diff.manufactuers <- vehicles$makes
is.data.frame(vehicles)
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
diff.manufactuers <- vehicles$make
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
unique(diff.manufactuers)
?unique()
# Filter the data set for vehicles manufactured in 1997
unique(diff.manufactuers, 1997)
# Filter the data set for vehicles manufactured in 1997
unique(diff.manufactuers, 1997)
# Filter the data set for vehicles manufactured in 1997
unique(diff.manufactuers, vehicles$year == 1997)
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
unique(diff.manufactuers)
# Filter the data set for vehicles manufactured in 1997
unique(diff.manufactuers[vehicles$year == 1997])
colnames(vehicles)
?order()
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
length(unique(diff.manufactuers))
# Filter the data set for vehicles manufactured in 1997
vehicles[vehicles$year == 1997, ])
# Filter the data set for vehicles manufactured in 1997
vehicles[vehicles$year == 1997, ]
# Filter the data set for vehicles manufactured in 1997
vehicles[vehicles$year == 1997]
# Filter the data set for vehicles manufactured in 1997
vehicles[vehicles$year == 1997]
# Filter the data set for vehicles manufactured in 1997
vehicles[vehicles$year == 1997, ]
unique(diff.manufactuers[vehicles$year == 1997])
car1997[order(car1997$hwy), ]
n 1997
car1997 <- vehicles[vehicles$year == 1997, ]
car1997[order(car1997$hwy), ]
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
car1997$average <- mean(cars1997$cty, cars1997$hwy)
car1997[order(car1997$hwy), ]
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
car1997$average <- mean(cars1997$cty, cars1997$hwy)
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
car1997$average <- mean(car1997$cty, cars1997$hwy)
# gas milage (between city and highway mpg) for each car
car1997$average <- mean(car1997$cty, cars1997$hwy)
car1997$average <- mean(car1997$cty, car1997$hwy)
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
car1997$average <- mean(car1997$cty, car1997$hwy, 1)
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
car1997$average <- (car1997$cty + car1997$hwy) / 2
car1997
car1997$average
# Install and load the "dplyr" library
install.packages("dplyr")
library("dplyr")
library(dplyr)
library(dplyr)
# Install and load the "dplyr" library
install.packages("dplyr")
library(dplyr)
library(dplyr)
library(dplyr)
# Select the different manufacturers (makes) of the cars in this data set.
# Use the `distinct()` function to determine how many different car manufacturers
# Install and load the "fueleconomy" package
#install.packages('devtools')
#devtools::install_github("hadley/fueleconomy")
library(fueleconomy)
# Install and load the "fueleconomy" package
#install.packages('devtools')
#devtools::install_github("hadley/fueleconomy")
library(fueleconomy)
>
1*1
library(dplyr)
# Install (if needed) and load the "dplyr" library
library(dplyr)
# Install (if needed) and load the "fueleconomy" package
install.packages('devtools')
devtools::install_github("hadley/fueleconomy")
